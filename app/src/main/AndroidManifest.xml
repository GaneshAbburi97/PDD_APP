<?xml version="1.0" encoding="utf-8"?>
<!--
  File: app/src/main/AndroidManifest.xml
  📌 App configuration and permissions
  📌 Activities, services, receivers declaration
  📌 AndroidX requirements
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ====================================================================
         PERMISSIONS - Network & File Access
         ==================================================================== -->

    <!-- ✅ REQUIRED: Internet access for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ✅ REQUIRED: Check network state -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ✅ File access permissions -->
    <!-- For Android 12 and below -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- For Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- For Android 14+ Selected Photos Access -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!-- Optional: Write to external storage (Android 10 and below) -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- ====================================================================
         FEATURES
         ==================================================================== -->

    <!-- Declare this app requires internet (optional but good practice) -->
    <uses-feature
        android:name="android.hardware.internet"
        android:required="true" />

    <!-- ====================================================================
         APPLICATION CONFIGURATION
         ==================================================================== -->

    <application
        android:name=".MedicalProcessorApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MedicalFileProcessor"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="35">

        <!-- ================================================================
             MAIN ACTIVITY
             ================================================================ -->
        <!--
          Entry point of the app
          android:exported="true" required for API 31+
          android:screenOrientation="portrait" forces portrait only
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.MedicalFileProcessor"
            android:screenOrientation="unspecified"
            android:windowSoftInputMode="adjustResize">

            <!-- Intent filter: Makes this the launcher activity -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- ================================================================
             BACKGROUND SERVICES (Optional - uncomment when needed)
             ================================================================ -->
        <!--
          Example service for file upload in background
          Uncomment when implementing background processing
        -->
        <!-- <service
            android:name=".service.FileUploadService"
            android:exported="false" /> -->

        <!-- ================================================================
             FILE PROVIDER (Optional - uncomment when needed)
             ================================================================ -->
        <!--
          Share files securely with other apps
          Uncomment when implementing file sharing
        -->
        <!-- <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.medical.fileprocessor.fileprovider"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider> -->

        <!-- ================================================================
             FIREBASE CONFIGURATION
             ================================================================ -->
        <!--
          Firebase Cloud Messaging receiver
          Handles push notifications
          Uncomment if using Firebase Cloud Messaging
        -->
        <!-- <receiver
            android:name="com.google.firebase.messaging.FirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </receiver> -->

        <!-- ================================================================
             OTHER METADATA
             ================================================================ -->
        <!--
          Google Play Services version compatibility
          Required for Firebase and other Google services
        -->
        <!-- <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" /> -->

    </application>

</manifest>